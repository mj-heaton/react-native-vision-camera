name: Build & Release VisionCamera Fork

on:
  push:
    branches: [ main ]
    tags:
      - "v*.*.*"

permissions:
  contents: write

concurrency:
  group: visioncamera-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # no cache here because there is no root lockfile

      # Install only the package workspace (no root lockfile required)
      - name: Install deps (package/)
        working-directory: package
        run: npm install

      - name: Build package
        working-directory: package
        run: npm run build

      - name: Verify plugin output exists
        run: |
          test -f package/app.plugin.js || (echo "Missing package/app.plugin.js" && exit 1)
          test -f package/lib/commonjs/expo-plugin/withVisionCamera.js || \
            (echo "❌ Missing compiled plugin at package/lib/commonjs/expo-plugin/withVisionCamera.js" && exit 1)

      - name: Pack tarball
        working-directory: package
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          echo "PKG_NAME=$PKG_NAME" >> $GITHUB_ENV
          echo "PKG_VERSION=$PKG_VERSION" >> $GITHUB_ENV
          TARBALL=$(npm pack --silent)
          echo "TARBALL=$TARBALL" >> $GITHUB_ENV
          mv "$TARBALL" "../react-native-vision-camera-${PKG_VERSION}.tgz"

      - name: Generate checksum
        run: |
          echo "ASSET_NAME=react-native-vision-camera-${PKG_VERSION}.tgz" >> $GITHUB_ENV
          shasum -a 256 "$ASSET_NAME" > "${ASSET_NAME}.sha256"

      - name: Create GitHub Release for tag
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        env:
          ASSET_NAME: ${{ env.ASSET_NAME }}
        with:
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            ${{ env.ASSET_NAME }}
            ${{ env.ASSET_NAME }}.sha256

      - name: Create/Update prerelease for main
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        env:
          ASSET_NAME: ${{ env.ASSET_NAME }}
        with:
          tag_name: latest-main
          name: Latest main
          draft: false
          prerelease: true
          generate_release_notes: false
          files: |
            ${{ env.ASSET_NAME }}
            ${{ env.ASSET_NAME }}.sha256
